{
  "name": "miniverse",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:all": "concurrently -n broker,backend,frontend -c \"yellow.bold,cyan.bold,green.bold\" \"npm run dev:broker\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:broker": "mosquitto -c mqtt_broker/config/mosquitto.conf -v",
    "dev:backend": "cd backend && cargo run",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:frontend", 
    "build:frontend": "cd frontend && npm run build",
    "install:all": "cd backend && cargo fetch && cd ../frontend && npm install",
    "clean": "cd backend && cargo clean && cd ../frontend && rm -rf node_modules dist .astro"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
